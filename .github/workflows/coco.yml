name: S6 COCO Benchmark

on:
  push:
    branches: [ "main" ]

jobs:
  run-benchmark:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake git python3-numpy

    - name: Clone COCO
      run: |
        git clone https://github.com/numbbo/coco.git

    - name: Build COCO Python interface
      run: |
        cd coco
        mkdir build
        cd build
        cmake ..
        make -j4

    - name: Install cocoex
      run: |
        cd coco/code-experiments/build/python
        pip install .

    - name: Run S6 COCO Benchmark
      run: |
        python coco_s6_benchmark.py

    - name: Upload Results
      uses: actions/upload-artifact@v4
      with:
        name: s6-coco-results
        path: S6_RESULTS/
