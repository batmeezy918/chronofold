# PROOF_GRAPH.md

## Theorem & Proof Dependency Graph

```
                   [Lean 4 Core Engine]
                             │
            ┌────────────────┴────────────────┐
            ▼                                 ▼
   [AGD.Verify.State]               [Chronofold.Auto.omega]
            │                                 │
   ┌────────┴────────┐               ┌────────┴────────┐
   ▼                 ▼               ▼                 ▼
[AGDEquiv]     [Admissible]   [omega_divides_n]  [omega_le_n]
   │                 │
   ▼                 ▼
[QStar]         [TBar_sound]
   │                 │
   ▼                 ▼
[uniqueMorph]  [admission_iff_descends]
   │
   ▼
[Replay]
   │
   ▼
[replay_preserves_invariants]
```

## Module Graph Inventory

### 1. `Verify.lean` (Namespace `AGD`)
- **`State`**: Operational state structure.
- **`Omega` / `Covariant`**: Decidable invariant and law functions.
- **`AGDEquiv`**: Equivalence relation generated by $(\Omega, C)$.
- **`agdSetoid`**: Setoid instance proving equivalence properties (`refl`, `symm`, `trans`).
- **`QStar`**: Minimal Quotient Type $Q^* = \text{State} / \text{AGDEquiv}$.
- **`TBar` / `TBar_sound`**: Quotient descent operator and soundness theorem.
- **`descends`**: Operator descent existence theorem.
- **`uniqueMorph` / `uniqueMorph_unique`**: Universal initiality theorem for $Q^*$.
- **`interchangeable` / `interchangeable_iff`**: State interchangeability equivalence theorem.
- **`admission_iff_descends`**: Complete characterization of operator admissibility via identity descent.
- **`ConstitutionalObject` / `Witness` / `Fiber` / `Registry` / `Compiler` / `Serialization` / `Hash` / `Builder`**: Constitutional metamodel.
- **`Replay` / `replay_preserves_invariants`**: Sequential operator replay and invariant preservation theorem.

### 2. `ChronoFold/Auto.lean` (Namespace `Chronofold`)
- **`rho_step`**: Quadratic congruential state step.
- **`omega`**: Algebraic probe operator $\text{gcd}(x^2 - x, n)$.
- **`omega_step`**: Probe augmented step.
- **`omega_divides_n`**: Divisibility theorem ($\omega(x,n) \mid n$).
- **`omega_nonneg`**: Nonnegativity theorem ($0 \le \omega(x,n)$).
- **`omega_le_n`**: Bound theorem ($\omega(x,n) \le n$ for $n > 0$).

### 3. Proven Intake Theorems
- **`THM_000001__smoke_test.lean`**: Validated intake proof for basic arithmetic soundness.
