# GitHub Workflow Audit (Phase 5)

All GitHub action workflows are enumerated and audited.
### Workflow: `.github/workflows/chronofold-auto.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/flutter.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/build.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/theorem-intake.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/coco.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/chronofold.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/pages.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/lean.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/snap.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).

### Workflow: `.github/workflows/snap-benchmark.yml`
- **Status:** Audited
- **Issues:** Non-pinned actions found (e.g. actions/checkout@v3 instead of SHA).
